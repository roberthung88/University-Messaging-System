import { createMuiTheme } from '@material-ui/core';

export const uiTheme = createMuiTheme({
  palette: {
    primary: {
      main: "#990000",
    },
    secondary: {
      main: "#FFCC00",
    }
  },
});